Flask==3.0.0
Werkzeug>=3.0.6,<3.1
requests==2.32.4
markdown==3.6
bleach==6.1.0
docker==7.1.0
psutil==5.9.6
google-api-python-client==2.137.0
google-auth==2.32.0
waitress>=3.0.0
Flask-Compress>=1.14
# BYOS renderer (byos.py): screenshot -> 1-bit; Liquid = the TRMNL plugin markups run unchanged
Pillow>=10.0
python-liquid>=2.0
websocket-client>=1.8
# Keep <-> restock join (keep_sync.py); pinned to the versions the token was minted with
gkeepapi==0.17.1
gpsoauth==2.0.0
